<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.dao.WeightDAO">

	<insert id="insertWeight" parameterType="WeightVO">
		INSERT INTO weight(weight, weightdate, email)
		VALUES (#{weight}, sysdate(),#{email})
	</insert>
	
	<select id="todayWeight" parameterType="UserVO" resultType="WeightVO">
		SELECT * FROM weight
		WHERE email = #{email}
		AND weightdate = curdate()
		ORDER BY weightdate desc
		LIMIT 1;
	</select>
	
	<update id="updateWeight" parameterType="weightVO">
		UPDATE weight
		SET weight= #{weight}
		WHERE email = #{email} and weightdate=curdate()
	</update>
	
	<select id="getWeights" resultType="WeightVO">
		SELECT * from weight
		WHERE email=#{email}
		ORDER BY weightdate DESC
		LIMIT 5;
	</select>
</mapper>
